type Notion {
  # Example field (placeholder)
  exampleField: Int
}

input CreateNotionInput {
  # Example field (placeholder)
  exampleField: Int
}

input UpdateNotionInput {
  id: Int!
}

type Query {
  notion: [Notion]!
  notion(id: Int!): Notion
}

type Mutation {
  createNotion(createNotionInput: CreateNotionInput!): Notion!
  updateNotion(updateNotionInput: UpdateNotionInput!): Notion!
  removeNotion(id: Int!): Notion
}
